--- @meta
--- @wallet-ignore

--- @class WalletTemplateInfo @Defines The template's properties when loaded into PZ-Wallet.
--- @field name string|nil @(Optional) The name of the template. (Uses the filename if nil)
--- @field version string|nil @(Optional) The PZ version that the template is designed for.
--- @field authors string[] | string | nil @(Optional) The author(s) of the template.
--- @field description string[]|'external'|string|nil @(Optional) The description of the template. Use 'external' to define the template description in an identical filename with the extension `.md`.
